const float_config = require("./float.config")
const pattern_config = require("./pattern.config")
const phase_config = require("./phase.config")
const configs = [float_config, pattern_config, phase_config]

module.exports = configs
